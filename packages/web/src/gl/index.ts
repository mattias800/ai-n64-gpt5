// WebGL adapter stub; will render from graphics IR later
export interface WebGLAdapterOptions { canvas: HTMLCanvasElement; }
export class WebGLAdapter {
  constructor(_opts: WebGLAdapterOptions) {}
}

